#include <stdio.h>

/**
 * main  - entry point
 * Return: Always 0 (success)
 */

int main(void)
{
	int b;
	/*prints a new line*/

	b = 0;
	while
		(b <= 10)
	{
		putchar(b);
		b++;
	}
	putchar('\n');
	return (0);
}
