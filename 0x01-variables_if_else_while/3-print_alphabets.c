#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Display the alphabet letters in uppercase
 *
 * Return: 0 (Run successful).
 *
 */

int main(void)
{
	char lower = 'a';
	char upper = 'A';
	
	for (a = 'a'; a <= 'z'; a++;
		{
			for (alphaB = 'A'; alphaB <= 'Z'; alphaB++)
			{
				putchar('A');
			}
			putchar('a');
			putchar('\n');
			return (0);
		}
}
