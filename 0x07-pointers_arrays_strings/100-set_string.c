#include "main.h"

/**
 * set_string - Takes a pointer to pointer to a character as arguments
 * @s: char pointing to a pointer
 * @to: char acting as argument
 */

#include <stdio.h>

void set_string(char **s, char *to)
{
	*s = to;
}
