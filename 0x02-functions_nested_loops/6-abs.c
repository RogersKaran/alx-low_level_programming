#include "main.c"

/**
 * _abs - Computing the absolute value of an integer
 * @c: The integer to be be completed
 * Return; Absolute value or zero
 */

int _abs(int c)
{
	if(c < 0)
	{
		int _abs_val;
		abs_val = c * -1;

		return (abs_val);
	}
	return (c);
}
